# PDF文档内容提取
> 提取时间: 2025-10-09 17:26:05
> 总页数: 7
> 处理页数: 7

## 第 1 页

矩阵求导与张量”坍缩”

1. 场景
考虑一个多层感知机(MLP)的中间层:
z=Wxb, y = a(z)
• x∈R":前一层输出(本层输入)
W∈Rmxn;权重矩阵
b∈R™:偏置
z∈Rm;线性变换结果
y∈R”:激活函数输出
激活函数可以是 sigmoid、ReLU等,逐元素作用。
目标:计算以进行梯度下降,其中L是损失函数(如交叉熵)。

2. 损失函数示例
最后一层输出经过 softmax 得到预测概率:
e
Y =
Σ
损失使用交叉熵(cross-entropy):
L=
Σ(9)
• 为真实标签(one-hot编码)
• 反向传播需要求梯度:8L/8W

3. 理论上的三维张量
对中间层z = Wz+b,严格地说:

• i=1..m输出索引
j=1..m-W行索引
k1..n-W列索引
这形成一个mxmxn的三维张量。
直观理解:
дуг
W

---

## 第 2 页

- 每个输出 $y_i$ 对应一页“梯度矩阵”
- 每页大小 $m \times n$, 存储它对 $W$ 所有元素的偏导
- 大部分元素为零，因为每个 $y_i$ 只依赖 $W$ 的对应行（线性关系）

4. 链式法则分量形式

对任意输出 $y_i$ 与 $W$ 元素 $W_{j,k}$:

$\frac{\partial y_i}{\partial W_{j,k}} = \sum_{r=1}^{m} \frac{\partial y_i}{\partial z_r} \frac{\partial z_r}{\partial W_{j,k}}$

4.1 计算 $\frac{\partial z_r}{\partial W_{j,k}}$

$z_r = \sum_{t=1}^{n} W_{r,t}x_t + b_r$

$\frac{\partial z_r}{\partial W_{j,k}} = \begin{cases} x_k & r = j \\ 0 & r \neq j \end{cases} = \delta_{rj}x_k$

- 这是一个稀疏张量，只有对应行的元素非零

4.2 代入求和

$\frac{\partial y_i}{\partial W_{j,k}} = \sum_{r=1}^{m} \frac{\partial y_i}{\partial z_r} \delta_{rj} x_k = \frac{\partial y_i}{\partial z_j} x_k$

解释:

- 对每个输出 $y_i$，它对 $W$ 的梯度只取决于第 $j$ 行的 $z$
- 对逐元素激活函数 (sigmoid、ReLU 等), $\frac{\partial y_i}{\partial z_j} = 0$ 如果 $i \neq j$, 否则是 $\sigma'(z_i)$
- 这就是为什么三维张量“几乎稀疏”，大部分元素为 0

5. 矩阵形式：外积表达

将所有输出 $i$ 和输入列 $k$ 堆叠:

$\frac{\partial \mathbf{y}}{\partial W} = \begin{bmatrix} \frac{\partial y_1}{\partial W_{:,1}} \\ \frac{\partial y_2}{\partial W_{:,2}} \\ \vdots \\ \frac{\partial y_m}{\partial W_{:,i}} \end{bmatrix} = \begin{bmatrix} \frac{\partial y_1}{\partial z_1} \mathbf{x}^T \\ \frac{\partial y_2}{\partial z_2} \mathbf{x}^T \\ \vdots \\ \frac{\partial y_m}{\partial z_m} \mathbf{x}^T \end{bmatrix} = (\frac{\partial \mathbf{y}}{\partial \mathbf{z}}) \mathbf{x}^T$

- $\frac{\partial \mathbf{y}}{\partial \mathbf{z}}$: 长度 $m$ 的列向量，每个元素是激活函数导数 $\sigma'(z_i)$
- $\mathbf{x}^T$: 行向量
- 外积得到 $m \times n$ 矩阵 $\rightarrow$ 反向传播梯度矩阵

---

## 第 3 页

6.“三维张量-矩阵”的坍缩过程

形象化理解:
1. 原始三维张量(m,m,n)
    * 第一维:输出索引i
    * 第二维:W行j
    * 第三维:W列k
    * 非零元素只在i=j行
2. 链式法则求和(收缩):
    ∂L/∂Wjk = Σ(∂L/∂yi * ∂yi/∂Wjk)
    * Kronecker delta 选择 i=j
    * 结果:每行只保留非零部分
3. 外积形式:
    * 每行梯度=对应输出导数×输入向量
    * 堆叠所有行->∂y/∂W = (∂y/∂z)x

7. 数值示例
假设:
* m = 2, n = 3
* 输入x = [1,2,3]T
* z = [0.1, -1.0]
* sigmoid 导数: σ'(0.1) ≈ 0.2494, σ'(1.0) ≈ 0.1966
外积:
∂y/∂W = [[0.2494], [0.1966]] * [1 2 3] = [[0.2494 0.4988 0.7482], [0.1966 0.3932 0.5898]]
每行对应输出对W的梯度。

8. 链式法则与矩阵求导的核心逻辑
1. 链式法则保证梯度可以按函数嵌套分解:
∂L/∂W = ∂L/∂y * ∂y/∂z * ∂z/∂W
2. 线性变换z=Wx+b:
    * ∂z/∂W的非零元素是x
    * 分量乘法+求和->直接形成外积

---

## 第 4 页

3. 稀疏性+求和 = 坍缩:
○ 原本三维张量不必实际存储
○ 外积形式即可完全表示梯度矩阵

9. 关键总结
• 对中间层线性变换 z = Wx + b:
○ 严格 ∂y/∂W 是三维张量
○ 线性 + 逐元素激活 - 大部分元素为零
• 链式法则收缩后 - 外积形式
○ 公式:
∂y/∂W = (∂y/∂z)xᵀ

• 理解要点:
1. 每个输出的梯度只依赖对应行的 W
2. 外积表达式 = 梯度向量 × 输入向量
3. 三维张量的“坍缩”实际上是链式法则 + 稀疏性的自然结果

曾神考我的问题的关键在于下面这个式子从第二行到第三行的推导:
dy/dW
= (dy dz)/(dz dW)
= dy/dz xᵀ

下面附上曾神的两种证明过程:

思路一: 使用爱因斯坦求和约定
dL/dW, L = f(y), y = Wx.
dL/dAₚ₂ = (dL/dyᵢ) (dyᵢ/dAₚ₂)
dyᵢ/dAₚ₂ = ∂(Aᵢⱼxⱼ)/∂Aₚ₂ = δᵢₚ δⱼ₂xⱼ = δᵢₚ x₂
dL/dAₚ₂ = (dL/dyᵢ) δᵢₚ x₂ = dL/dyₚ x₂ => dL/dA = dL/dy L.Xᵀ

---

## 第 5 页

证明:
1. 定义: L = f(y) y = Wx
2. 链式法则: 根据链式法则,我们可以将L对W的导数分解为: =
补充:这里使用了爱因斯坦求和约定,对重复索引i进行求和。我在N-20230217.pdf中学过了这一块,但是现在忘了。
3.计算:
4.计算:=(W); Wij (同样使用爱因斯坦求和约定)所以,当W矩阵的元素被表示为 Aij 时: Y = A
= Sipdjqxj = dip2q
补充:这里的 8ip 和jq 是 Kronecker delta 符号。当i=p且j=q时为1,否则为0。
所以实际上是在询问当i=p时Apjj对Wpq的偏导,结果就是q.
5. 代回链式法则:=(dipxq) =
q
补充:根据 Kronecker delta 的性质,ip会使得所有i≠p的项为零,只保留i=p的项,所以结果是业。
6. 将结果写成矩阵形式:观察结果,这表示对W矩阵的第(p,q)个元素的导数。将其组织成矩阵形式,的第(p,q)个元素就是。这意味着=
()()。所以,= ()
补充:
• 在一般情况下,如果是一个标量,是一个向量(N×1), W是一个矩阵(M×N),是一个向量(N×1)。那么应该是一个M×N的矩阵。
• 当计算时,得到的是一个1×N的行向量(或转置后是N×1的列向量)。
• 当计算时,得到的是一个标量(当=p时)。
• 最终。如果我们将看作一个行向量,看作一个行向量,那么它们的“外积”会形成一个矩阵。
• 曾神提到“线性函数导致求和的式子里出现了两个kronecker符号,就那个导致最终结果只有1维了”。这可能是由于在将最终的矩阵形式写出来时,对于特定情况或误解了矩阵乘法的维度。在正确的矩阵求导中,如果是标量,W是矩阵,那么导数应该是一个与W同维度的矩阵。
• 曾神还提到“在一般的情形中,虽然维度消失了,但由于不能得知函数的表达式,所以写出来不会发生任何变化”。这也许暗示了在某些中间步骤中,为了简化表示,导致了维度的隐含变化(?)
• “对于线性函数,用Kronecker乘积展开它,会出现一个单位矩阵,这个单位矩阵可以与向量合并”,说明了 Kronecker 乘积在处理线性函数和维度变化中的作用。

思路二:使用向量化和 Kronecker 乘积

---

## 第 6 页

∂y/∂vec(w) = ∂y/∂z * ∂z/∂vec(w)
=∇(∂z/∂vec(w))
= [1xn] nxnm
= [1xnm]

y = f(z)
z = g(x)
y: setup scalar
z: vector (scalar)
U: matrix (nxm)
∂y/∂z = [∂y/∂z1, ..., ∂y/∂zn] 记为∇
∂z/∂vec(w) = [∂z1/∂vec(w), ..., ∂zn/∂vec(w)]
∂a/∂b = [ ] mn矩阵

如果 z = Wx
x = [x1, ..., xm]
∂z/∂vec(w) = (x^T⊗In)
∂z/∂vec(w) = x^T⊗In
代回上式：∂y/∂vec(w) = ∇(x^T⊗In)
= ∇[x1In, x2In, ..., xmIn]
= [x1∇, x2∇, ..., xm∇]
= (vec(∇x))^T
=> ∂y/∂w = ∇x = ∂y/∂z x

证明：
1. 定义：y = f(z) z = Wx
2. 链式法则(向量化形式)：∂y/∂vec(w) = ∂y/∂z * ∂z/∂vec(w)
补充：这里vec(W)是将矩阵W按列堆叠成一个列向量。∂y/∂z会是一个行向量(如果y是标量，z是列向量)。
3. 计算∂y/∂z：如果y是标量，z是m×1的向量，那么∂y/∂z是一个1×m的行向量：∂y/∂z = [∂y/∂z1, ..., ∂y/∂zm]
4. 计算∂z/∂vec(w)：z = Wx我们将z的每个元素展开，假设W是m×n矩阵，x是n×1向量。zi = ∑j=1 Wijxj
现在考虑vec(W)。假设W有m×n个元素。∂z/∂vec(w)将是一个大的矩阵，它的每一行对应z的一个元素，每一列对应vec(W)的一个元素。

---

## 第 7 页

补充:
我们知道 vec(ABC) = (Cᵀ ⊗ A)vec(B)。这里 Z = Wx = WIₙₓ (将 x 视为 Iₙₓ)
方便应用规则)。或者更直接地, Z - Wx 可以看作是一个线性变换。vec(Z) - vec(Wx)。

为了计算 ∂z/∂vec(W), 我们也可以使用以下性质: ∂(Ax)/∂x = Aᵀ (当 A 为常数矩阵时)
∂(xᵀA)/∂x = A

但这里是对 W 求导, 且 W 是矩阵。从笔记中可以看出, 为了处理 Z = Wx, 曾神使用
了 Kronecker 乘积和向量化: Z = Wx vec(Z) = vec(Wx) 可以写成 vec(Z) = (xᵀ ⊗ Iₘ) vec(W)。因此, ∂vec(Z)/∂vec(W) = (xᵀ ⊗ Iₘ)

补充: 这里 Iₘ 是 m × m 的单位矩阵。xᵀ ⊗ Iₘ 的维度是 m × (mn)。
5. 代回链式法则: ∂y/∂vec(W) = ∂y/∂z (xᵀ ⊗ Iₘ)

补充: ∂y/∂z 是一个 1 × m 的行向量, (xᵀ ⊗ Iₘ) 是一个 m × (mn) 的矩阵。最终结果是
一个 1 × (mn) 的行向量, 这正是 ∂y/∂vec(W) 的形式。

6. 将结果还原为矩阵形式(如果需要): 如果 ∂y/∂vec(W) 是一个 1 × (mn) 的行向量, 我们可以
通过逆向量化操作将其还原为 m × n 的矩阵 ∂y/∂W。通过一些线性代数的性质, 可以证明 ∂y/∂W = ∂y/∂z xᵀ。这与思路一的结果一致。

笔记的推导过程: ∂y/∂vec(W) = ∂y/∂z ∂z/∂vec(W) 其中 ∂y/∂z = v̄ (行向量)。∂z/∂vec(W) = (xᵀ ⊗ Iₘ)
(笔记中写成 Iₙ, 但根据维度应该是 Iₘ) v̄(∂z/∂vec(W)) = v̄(xᵀ ⊗ Iₘ) = [v̄₁x₁, v̄₂x₂, ..., v̄ₘxₙ]
这应该是将 v̄ 与 x 的每个元素相乘, 然后堆叠起来。

笔记的最后一步: ∂y/∂W => v̄X̄ = ∂y/∂z xᵀ 这里 v̄ 是 ∂y/∂z, X̄ 是 xᵀ。所以 ∂y/∂W = ∂y/∂z xᵀ。

补充:
* 第二种思路使用向量化和 Kronecker 乘积, 更加形式化和严谨地处理了矩阵求导, 对我而言比较难理解。
* “第三行相比于第二行, 可以认为是提前把这个矩阵乘法算了一部分”。这是对核心问题的更严谨的解释。在推导中, 通过对 Z = Wx 进行向量化处理, 其实已经隐含地进行了部分矩阵乘法运算。
* “所以维度消失的现象体现在了外部”, 这也许意味着在向量化过程中, 虽然中间表示的维度增加了(如 vec(W)), 但最终的导数结果在还原为矩阵形式时, 其维度与原始矩阵 W 保持一致。

总结
相同结论: ∂y/∂W = (∂y/∂z)xᵀ。

* 思路一(爱因斯坦求和约定): 侧重于分量级别的推导, 通过 Kronecker delta 处理索引, 最终将结果组合成矩阵形式。这种方法比较物理, 应该是在张量分析中常用的方法, 也是我比较好接受的。
* 思路二(向量化和 Kronecker 乘积): 侧重于整体矩阵和向量的运算, 通过向量化将矩阵转换为向量, 再利用 Kronecker 乘积的性质进行求导。这种方法据说在机器学习和统计学中对矩阵微积分的推导非常有用。

---

